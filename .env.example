# ══════════════════════════════════════════════
#  FinPlan — Monorepo Environment Variables
#  Copie para .env e preencha os valores
# ══════════════════════════════════════════════

# ── Compose project name ─────────────────────
COMPOSE_PROJECT_NAME=finplan

# ── Backend ──────────────────────────────────
APP_NAME="Financial Planning API"
APP_VERSION="1.0.0"
APP_ENV="development"          # development | staging | production
DEBUG=true
WORKERS=1

# Database (SQLite para dev; PostgreSQL para prod)
DATABASE_URL="sqlite:///app/data/financial.db"
# DATABASE_URL="postgres://finplan:finplan_pass@db:5432/finplan_db"

# JWT — gere com: openssl rand -hex 32
SECRET_KEY="TROQUE_POR_CHAVE_FORTE_MIN_32_CHARS_xxxxxxxxxxx"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS — em prod aponte para o domínio real
ALLOWED_ORIGINS="http://localhost"
ALLOWED_METHODS="GET,POST,PUT,PATCH,DELETE,OPTIONS"
ALLOWED_HEADERS="*"

RATE_LIMIT_PER_MINUTE=60

# Zillow integration
ZILLOW_API_KEY=""
ZILLOW_API_URL="https://api.zillow.com/v2"

# SSN encryption — gere com:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=""

LOG_LEVEL="INFO"
LOG_FORMAT="json"

# ── Frontend ─────────────────────────────────
# Injetado no config.js pelo entrypoint do container nginx
# Em dev: aponta para /api/v1 (proxy pelo nginx)
# Em prod: pode ser a URL pública da API
API_URL="/api/v1"
